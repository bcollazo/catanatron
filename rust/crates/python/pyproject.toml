[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "catanatron-rust"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["numpy>=1.16"]

[tool.maturin]
module-name = "catanatron_rust._rust"
python-source = "python"
