[package]
name = "catanatron-bench"
version = "0.1.0"
edition = "2021"

[dependencies]
catanatron-core = { path = "../core" }
catanatron-search = { path = "../search" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
