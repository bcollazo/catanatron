[package]
name = "catanatron-bot"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
catanatron-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
