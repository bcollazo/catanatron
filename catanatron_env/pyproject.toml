[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["catanatron_env"]

[project]
name = "catanatron_env"
version = "0.0.1"
dependencies = ["gymnasium", "pre-commit", "catanatron", "numpy"]
