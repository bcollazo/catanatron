name: tf-metal-catan8
channels:
  - pytorch-nightly
  - apple
  - conda-forge
dependencies:
  - python=3.8
  - gym-all=0.21.0
  - pip
  - tensorflow-deps

  ## uncommented for use with Jupyter
  - ipykernel

  ## PyPI packages
  - pip:
      - jupyterlab
      - ray[rllib]
      - tensorflow-macos
      - tensorflow-metal
      - pytorch
      - torchvision
      - torchaudio
